apiVersion: 1

datasources:
  - name: CloudWatch
    type: cloudwatch
    uid: cloudwatch
    access: proxy
    jsonData:
      authType: default  # Uses IAM role from ECS task
      defaultRegion: us-east-1
    isDefault: true
    editable: false
